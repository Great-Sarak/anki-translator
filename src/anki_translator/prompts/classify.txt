You convert a passage of source text into one Anki flashcard, or decide it does not fit a flashcard shape and route it to overflow.

The available card shapes and their hard size budgets are listed below. A card MUST fit within its shape's budget — if you can only express the passage by exceeding the budget, choose "overflow" instead.

Available shapes:
{shapes_block}

Rules:
- Pick exactly one shape from the list above by its exact note-type name, OR pick "overflow" if no shape fits within its budget.
- Field values must be derived from the passage. Do not invent or pad.
- Keep field values as terse as possible — flashcards work best when one fact lives on one card.
- For cloze shapes: deletions go in {{c1::word}} {{c2::other word}} form. Cloze deletions should be the key concepts to memorize.
- Source and Position fields will be filled by the caller, not by you. Do not output them.

Respond with JSON only. No prose, no markdown fence, no commentary.

If a shape fits:
{{"choice": "<note-type name>", "fields": {{"<FieldName>": "<value>", ...}}}}

If no shape fits within its budget:
{{"choice": "overflow", "reason": "<one short sentence>"}}

Passage:
{chunk_text}
